# Tasks

## 1. Runtime 分类与策略基线

- [x] 1.1 梳理当前平台判定代码路径，确认 `web-site`、`desktop-app`、`mobile-app` 三态来源。
- [x] 1.2 定义统一的入口策略矩阵（audio/deepseek/visitors × runtime）。
- [x] 1.3 确认策略默认值：Web 与 Desktop 保持现状，Mobile 首发阶段隐藏三类入口。

## 2. 入口治理接入

- [x] 2.1 在音频入口挂载点接入策略判定，仅移动端隐藏入口。
- [x] 2.2 在 DeepSeek 入口链路接入策略判定，仅移动端隐藏入口。
- [x] 2.3 在访问统计入口接入策略判定，仅移动端隐藏入口。
- [x] 2.4 确认入口隐藏时不会触发不必要的第三方脚本/网络初始化。

## 3. 回归验证

- [x] 3.1 Web 站点回归：验证三类入口可见性与当前线上一致。
- [ ] 3.2 Desktop 回归：验证音频、DeepSeek 不受影响；访问统计保持既有表现。
- [ ] 3.3 Mobile 回归：验证三类入口隐藏且页面主体学习流程可用。
- [x] 3.4 记录跨端验证结果，形成发布前检查凭证。

## 4. 发布与后续

- [x] 4.1 在发布说明中记录“移动端临时隐藏入口”范围。
- [x] 4.2 明确后续恢复策略（按功能分批恢复入口）与触发条件。
